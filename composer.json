{
    "name": "scms/backend",
    "description": "SCMS Backend API",
    "type": "project",
    "require": {
        "php": ">=7.4"
    },
    "scripts": {
        "post-install-cmd": [
            "php -r \"copy('.env.example', '.env');\""
        ]
    },
    "config": {
        "optimize-autoloader": true
    }
}
